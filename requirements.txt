# Dependencies only - Kept for backwards compatibility
# For new installations, use: pip install .

# Core dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.24.0
